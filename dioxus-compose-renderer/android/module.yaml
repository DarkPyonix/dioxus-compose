product: android/app

dependencies:
  - $compose.material3
  - $libs.androidx.activity.compose
  - $compose.preview
  - $compose.uiTooling

settings:
  compose: enabled
  android:
    namespace: dioxus.compose
    applicationId: dioxus.compose
